{
  "name": "@warden/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared WARDEN artifacts (contract ABIs) consumed by the agent, API, and web packages.",
  "files": [
    "abi"
  ],
  "exports": {
    "./abi/Guard.json": "./abi/Guard.json",
    "./abi/IGuard.json": "./abi/IGuard.json"
  },
  "scripts": {
    "build": "echo \"@warden/shared: no build needed\" && exit 0",
    "typecheck": "echo \"@warden/shared: no typecheck needed\" && exit 0",
    "test": "echo \"@warden/shared: no tests\" && exit 0"
  }
}
